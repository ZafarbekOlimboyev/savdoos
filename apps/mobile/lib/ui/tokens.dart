/// Layout tokens shared by the UI kit (touch-first, 390 dp wide phones).
library;

/// Minimum touch target (Material / WCAG): every tappable control is at least
/// this tall and wide.
const double kMinTouch = 48;

/// Height of the primary action button in a [StickyActionBar].
const double kPrimaryButtonHeight = 52;

/// Horizontal screen gutter.
const double kGutter = 16;

/// Standard corner radius of cards, fields and sheets.
const double kRadius = 14;
