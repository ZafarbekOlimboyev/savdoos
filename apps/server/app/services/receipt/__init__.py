"""Chek: shablon sozlamasi, logo, kanonik DTO, shtrix-kod/QR va chop etish jurnali (Phase 5F).

⚠️  SOTUV YOZISH YO'LIDAN MUSTAQIL. `services/sales.py`, `POST /sales`, `POST /returns`
    va `/sync/push` bu paketni IMPORT QILMAYDI: chek va chop etish — commit'dan keyingi
    alohida so'rov. Chek kodi yiqilsa ham sotuv yoziladi (`tests/test_receipt_dto.py`).
    Paket ataylab bo'sh `__init__` — import qilish hech narsani yuklamaydi.
"""
