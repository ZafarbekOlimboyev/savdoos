# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / Electron
node_modules/
dist/
dist-electron/
release/
.vite/

# Env / secrets
.env
*.local

# DB / data
*.sqlite
*.sqlite3
*.db
data/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Design manba (ixtiyoriy)
*.zip
.vendor_key.txt
apps/mobile/android/.kotlin/
# Android imzolash sirlari (Play release kaliti — HECH QACHON commit qilinmaydi)
apps/mobile/android/key.properties
apps/mobile/android/app/*.keystore
# Playwright E2E artefaktlari
test-results/
playwright-report/
apps/server/_e2e.db
apps/server/qa_*_results.json

# ── Baza nusxalari — HECH QACHON repoga tushmasin ────────────────────────────
# `tools/backup.py` va `scripts/backup_postgres.sh` shu yerga yozadi. Bu fayllar BUTUN
# production bazasi: barcha do'konlarning mijozlari, telefonlari, qarzlari va naqd ledgeri.
# CLAUDE.md dagi "push qil" qoidasi `git add -A` bajaradi — ignore bo'lmasa bitta buyruq
# butun bazani git tarixiga abadiy joylab qo'yardi (tarixdan o'chirish esa force-push talab
# qiladi va allaqachon klonlangan nusxalarni qaytarib bo'lmaydi).
backups/
*.dump
*.dump.sha256
*.dump.age
fingerprint.json
before.json
after.json
